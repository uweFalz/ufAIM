# Third-party notices

The authoritative resolved component list, versions, integrity hashes, and licenses are in `package-lock.json` and `sbom.cdx.json`.

- `mdb-reader` 3.2.0 — MIT — Copyright Andi Pätzold.
- `buffer`, `events`, `process`, and `stream-browserify` — MIT browser compatibility packages.
- `esbuild` is a build-only dependency and is not loaded by the application at runtime.

Redistribution of the generated Worker must retain the license comment block emitted at the end of `mdb-reader-worker-3.2.0.js` and the notices associated with all bundled transitive components.
