import assert from "node:assert/strict";
import test from "node:test";
import { ExistingAlignmentIntelligenceView } from "../../../app/view/workspace/ExistingAlignmentIntelligenceView.js";
function node(documentRef) { return { ownerDocument: documentRef, dataset: {}, children: [], hidden: false, append(...x) { this.children.push(...x); }, replaceChildren(...x) { this.children = x; }, addEventListener(type, fn) { this[type] = fn; }, querySelector(selector) { return selector === "[data-alignment-design-session]" ? this.children.find((x) => Object.hasOwn(x.dataset, "alignmentDesignSession")) ?? null : null; } }; }
test("visible board renders exact context and five existing dock actions", () => { const calls = [], documentRef = { createElement: () => node(documentRef) }, root = node(documentRef), view = new ExistingAlignmentIntelligenceView({ documentRef, actions: { openHorizontal: (x) => calls.push(x), openVertical() {}, openCant() {}, openChainage() {}, openReview() {} } }); view.renderDesignSessionBoard(root, { status: "active", mode: "q", context: { objectId: "A", revision: 4, s: 25 }, areas: ["horizontal", "vertical", "cant", "chainage", "source"].map((id) => ({ id, label: id, status: "not-covered", count: null, selectedElementId: id === "horizontal" ? "H1" : null, action: id === "source" ? "openReview" : `open${id[0].toUpperCase()}${id.slice(1)}`, enabled: true, provenancePresent: false })) }); const section = root.children[0], list = section.children[1]; assert.match(section.children[0].children[1].textContent, /A · revision 4 · s 25/); assert.equal(list.children.length, 5); list.children[0].children[3].click(); assert.deepEqual(calls[0], { source: "design-session", objectId: "A", discipline: "horizontal", elementId: "H1" }); });
