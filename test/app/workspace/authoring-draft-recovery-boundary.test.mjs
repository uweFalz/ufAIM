import assert from"node:assert/strict";import fs from"node:fs";import test from"node:test";const read=p=>fs.readFileSync(new URL(`../../../${p}`,import.meta.url),"utf8");
test("draft recovery is window-local and does not alter canonical authorities",()=>{const draft=read("app/domain/workspace/createAuthoringDraftStore.js"),horizontal=read("app/controllers/bridges/alignmentEditorBridge.js"),controllers=["VerticalProfile","Cant","Chainage"].map(name=>read(`app/controllers/alignment-profile/create${name}AuthoringDockController.js`)).join("\n");assert.match(draft,/new Map\(\)/);assert.doesNotMatch(draft,/localStorage|sessionStorage|indexedDB|sendCmd|history|audit/i);assert.match(draft,/JSON\.stringify/);assert.match(draft,/objectId.*discipline.*action/);assert.match(horizontal,/clearDraftAfterCanonicalRefresh/);assert.match(horizontal,/stop:\(\)=>drafts\.clearAll/);assert.match(controllers,/isConfirmedAuthoringSaveResult/);for(const forbidden of[/defaultValue|defaultDraft/i,/auto.?correct/i])assert.doesNotMatch(draft,forbidden);});
