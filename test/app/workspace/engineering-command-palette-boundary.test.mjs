import assert from"node:assert/strict";import fs from"node:fs";import test from"node:test";const read=p=>fs.readFileSync(new URL(`../../../${p}`,import.meta.url),"utf8");
test("palette only delegates existing actions and is responsive tool-surface compatible",()=>{const model=read("app/domain/workspace/buildEngineeringCommandPaletteModel.js"),controller=read("app/controllers/workspace/createEngineeringCommandPaletteController.js"),shell=read("app/view/shell/buildWindowShell.js"),init=read("app/runtime/init/initFeatures.js");for(const forbidden of[/sendCmd|localStorage|indexedDB|history|\bAI\b|geometry|autoaction/i])assert.doesNotMatch(model,forbidden);assert.match(shell,/btnCommandPalette/);assert.match(shell,/data-tool-kind="command-palette"/);assert.match(controller,/ctrlKey \|\| event\.metaKey/);assert.match(controller,/watchToolSurface/);assert.match(controller,/busy/);for(const action of["activateMain","activateQ","activateL","openWorkbench","openObjects","openTasks","openHorizontal","openVertical","openCant","openChainage","createAlignment"])assert.match(init,new RegExp(action));});
test("desktop palette rules precede the mobile media block",()=>{const css=read("app/styles/app.css"),mobile=css.indexOf("@media (max-width: 760px)"),common=["#engineeringCommandPaletteOverlay[data-tool-presentation=\"dock\"]","#engineeringCommandPaletteBody ul","#engineeringCommandPaletteBody li","#engineeringCommandPaletteBody button"];assert.ok(mobile>0);for(const selector of common){const index=css.indexOf(selector);assert.ok(index>=0&&index<mobile,`${selector} must be desktop/common`);}const sheet=css.indexOf('#overlay-root > [data-tool-surface][data-tool-presentation="sheet"]');assert.ok(sheet>mobile,"sheet override remains mobile-only");});
