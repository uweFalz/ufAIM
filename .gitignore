# ------------------------------------------------------------
# OS / Editor noise
# ------------------------------------------------------------
.DS_Store
**/.DS_Store
.metadata/
*.swp
*.swo
*~
*.tmp

# --- macOS bundles / maps (optional) ---
*.dSYM

# ------------------------------------------------------------
# Git housekeeping
# ------------------------------------------------------------
.gitignore
app/.gitignore

# ------------------------------------------------------------
# Scratch / experiments / local helpers
# ------------------------------------------------------------
var/
samples/
*.sh
var/
dump_repo_full.sh
setup_structure.sh
**/empty.js
AGENTS.md
**/AGENTS.md

# ------------------------------------------------------------
# Docs
# ------------------------------------------------------------
docs/archive/
bib/

# ------------------------------------------------------------
# LaTeX build artefacts (historic but still useful)
# ------------------------------------------------------------
*.aux
*.out
*.toc
*.synctex.gz
*.run.xml
*.fdb_latexmk
*.log

# ------------------------------------------------------------
# Build / runtime artefacts (Web / JS)
# ------------------------------------------------------------
node_modules/
dist/
build/
coverage/
*.map

# temporary bundles / local dumps
*.tgz
*.tar.gz

# ------------------------------------------------------------
# Logs
# ------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local tooling / dumps / scratch
scripts/
samples/
sample0/
var/
dump_repo_full.sh
setup_structure.sh

# big legacy depots / staging parks
_legacy/
**/_legacy/
**/_dormant/
**/_graveyard/

# --- Local scratch / outputs ---
var/
.metadata/
# (samples/ NICHT ignorieren, weil du ggf. echte Samples willst)

# --- Archives / big legacy dumps ---
**/legacy/
_legacy/   # wenn du das dauerhaft als "externes Altarchiv" halten willst
# Falls _legacy im Repo bleiben soll, aber nicht versioniert:
# -> dann so lassen. Wenn _legacy versioniert werden soll: diese Zeile entfernen.

# --- Node-ish (falls später) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
