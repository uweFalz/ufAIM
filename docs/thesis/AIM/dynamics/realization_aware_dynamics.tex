% dynamics/realization_aware_dynamics.tex

\chapter{Realization-Aware Runtime Dynamics}

\label{chap:realization-aware-dynamics}

% ============================================================
\section*{Motivation}
% ============================================================

Operational railway behaviour is commonly derived from analytical target
geometry.

Within AIM, this interpretation is incomplete.

Vehicles do not interact with target geometry. They interact with
realized railway states reconstructed and evaluated during runtime.

The relevant chain is therefore

\[
\mathrm{pose3}_{\mathrm{target}}
\rightarrow
\mathcal R
\rightarrow
\mathrm{pose3}_{\mathrm{real}}
\rightarrow
\mathcal D
\rightarrow
\mathrm{runtime}.
\]

Dynamic behaviour emerges from realized railway states rather than from
idealized design geometry alone.

\begin{principle}[Realization-aware dynamics principle]
\label{princ:realization-aware-dynamics}
Within AIM, operational railway dynamics are generated from realized
runtime states rather than directly from analytical target geometry.
\end{principle}

% ============================================================
\section{Realized Runtime States}
% ============================================================

\begin{definition}[Realized runtime state]
\label{def:realized-runtime-state}
A realized runtime state is a railway state evaluated from realized
rather than idealized geometry.
\end{definition}

Within AIM, the operationally relevant state is

\[
\mathrm{pose3}_{\mathrm{real}}(s).
\]

This state is obtained through realization, reconstruction, and runtime
evaluation.

The analytical target state remains important, but only through the
realized state that ultimately governs operational behaviour.

% ============================================================
\section{Realization Filtering}
% ============================================================

Realization alters the states from which operational behaviour emerges.

Construction tolerances, maintenance history, measurement uncertainty,
and reconstruction procedures modify the effective railway state
available during operation.

Consequently, realization acts as a filter between design intent and
runtime behaviour.

\begin{proposition}[Realization-filter principle]
\label{prop:realization-filter-principle}
Operational behaviour is generated from realized states rather than
directly from analytical target states.
\end{proposition}

A design concept is operationally relevant only if its intended runtime
behaviour survives realization.

% ============================================================
\section{Realization-Aware Admissibility}
% ============================================================

\begin{definition}[Realization-aware admissibility]
\label{def:realization-aware-admissibility}
A railway state is realization-aware admissible if its realized runtime
behaviour remains inside the relevant operational envelope.
\end{definition}

Admissibility therefore depends not only on design geometry but also on
the realized state that emerges after construction, reconstruction, and
runtime evaluation.

Operational admissibility is consequently a runtime property rather than
a purely geometric property.

% ============================================================
\section{Runtime Envelopes}
% ============================================================

\begin{definition}[Runtime envelope]
\label{def:runtime-envelope}
A runtime envelope is the admissible region of realized operational
states.
\end{definition}

Runtime envelopes define the operational limits within which realized
states are considered acceptable.

Examples include comfort limits, dynamic admissibility limits, and other
project-specific operational constraints.

Runtime envelopes provide the connection between realized states and
engineering evaluation.

% ============================================================
\section{Realization-Aware State Design}
% ============================================================

The realization-aware viewpoint changes the engineering objective.

Railway alignment design is no longer interpreted merely as the design
of analytical geometry.

Instead, it becomes the design of realized operational states.

The central engineering question becomes:

\begin{quote}
Which realized operational state should emerge during operation?
\end{quote}

This shifts attention from target geometry toward the behaviour
ultimately experienced in reality.

% ============================================================
\section{Canonical Interpretation}
% ============================================================

\begin{proposition}
Operational railway behaviour emerges from realized runtime states
rather than directly from analytical target geometry.
\end{proposition}

\begin{proposition}
Realization acts as a state-generating operator between design intent
and operation.
\end{proposition}

\begin{proposition}
Admissibility is fundamentally a realization-aware property.
\end{proposition}

\begin{proposition}
Within AIM, railway alignment design is interpreted as realization-aware
state design.
\end{proposition}

% ============================================================
\section{Connection to AIM}
% ============================================================

\begin{summary}
Realization-aware dynamics establish the connection between realization
and operational behaviour.

Within AIM:

\begin{itemize}
\item operational behaviour is generated from realized states,
\item realization filters design intent,
\item admissibility becomes realization-aware,
\item runtime envelopes define operational limits,
\item and railway alignment becomes realization-aware state design.
\end{itemize}

This chapter therefore provides the conceptual bridge between
realization, runtime interpretation, engineering admissibility, and
optimization.
\end{summary}
