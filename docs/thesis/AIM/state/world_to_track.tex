% state/world_to_track.tex

\chapter{World--Track Coordinate Transformation}

\label{chap:world-to-track}

\index{Projection!world-track coordinate transformation}
\index{Reference system!track space}

% ============================================================
\section*{Motivation}
% ============================================================

This chapter answers how intrinsic railway coordinates and external
world coordinates are related in AIM without collapsing geometry,
runtime evaluation, and coordinate representation into one operation.
It establishes why world--track transformation is treated as an
operator-level runtime service and what this implies for projection,
ambiguity, and engineering interpretation.

The first question is directional: what exactly is mapped from world to
track, and what is mapped from track to world?

% state/world_to_track.tex
\begin{figure}[htbp]
\centering
\input{figures/runtime/support_world_to_track_operator}
\caption{Supporting operator view: world-to-track transformation as directed runtime mapping.}
\label{fig:world-to-track-transformation}
\end{figure}

The figure fixes this directionality before formal definitions are
introduced and prevents confusion between projection and generic
coordinate conversion.

Railway alignments are formulated intrinsically using arc-length-based
coordinates. Real-world data, by contrast, is typically represented in
external coordinate reference systems such as WGS84, UTM,
Gauss--Krüger, DBRef, or local engineering CRS definitions. A
transformation between intrinsic alignment coordinates and external world
coordinates is therefore required. Within AIM, this transformation is
interpreted as a runtime evaluation service connecting intrinsic
alignment states, measured data, visualization, realization, topology,
and operational geometry.

\begin{principle}[Intrinsic-space primacy]
\label{princ:intrinsic-space-primacy}
\index{Intrinsic-space primacy principle}
Within AIM, intrinsic railway geometry is primary.

World-space coordinates are interpreted as external embeddings generated
through runtime evaluation.
\end{principle}

% ============================================================
\section{Track Coordinate System}
% ============================================================

\begin{definition}[Track space]
\label{def:track-space}
\index{Track space}
The track space is the intrinsic railway coordinate domain:
\[
\Omega_{\mathrm{track}}
=
\{(s,d,h)\}.
\]
It represents positions relative to an alignment rather than positions
in an external coordinate reference system.
\end{definition}

\begin{definition}[Track coordinate system]
\label{def:track-coordinate-system}
\index{Coordinate system!track}
The intrinsic track coordinate system consists of:
\[
(s,d,h),
\]
where:
\begin{itemize}
\item \(s\) denotes intrinsic arc-length,
\item \(d\) denotes lateral offset,
\item \(h\) denotes vertical offset.
\end{itemize}
\end{definition}

Track coordinates belong to the intrinsic track-space domain
\(\Omega_{\mathrm{track}}\), introduced in \cref{def:track-space}. In
planar situations, only \((s,d)\) is required. Track coordinates are
intrinsic alignment coordinates and must not be confused with projected
world coordinates.

% ------------------------------------------------------------

\subsection{Intrinsic Geometry versus External Coordinates}

Intrinsic geometry is independent of any external coordinate reference
system. The same intrinsic alignment may be embedded into projected
engineering planes, ellipsoidal world models, local realization frames,
or visualization coordinate systems. Thus, intrinsic alignment geometry
and external CRS representation form distinct conceptual layers.

% ------------------------------------------------------------

\subsection{Arc-Length and Stationing}

\index{Arc-length!vs stationing}

Intrinsic arc-length is not identical to engineering stationing.
Operational kilometre systems may contain station equations, kilometre
jumps, overlapping station ranges, topology-dependent references, and
operational branch relations. Stationing systems are operational
reference structures layered on top of intrinsic geometry. Consequently,
the mapping \(s \leftrightarrow \mathrm{km}\) is generally neither linear
nor globally unique.

% ============================================================
\section{Track-to-World Transformation}
% ============================================================

\begin{definition}[Track-to-world mapping]
\label{def:state-track-to-world-runtime}
\index{Projection!track-to-world}
The forward transformation is:
\begin{equation}
x(s,d)
=
\gamma(s)+d\,\mathbf n(s).
\label{eq:state-track-to-world-runtime}
\end{equation}
\end{definition}

This relation specializes the canonical track-to-world operator
\(\mathcal P_{\mathrm{tw}}\) introduced in
\cref{def:track-to-world-operator}.

The complementary question is then geometric-operational: how is the
intrinsic state emitted into world space once directionality is fixed?

\begin{figure}[htbp]
\centering
\input{figures/runtime/support_track_to_world_operator}
\caption{Supporting operator view: track-to-world transformation as the complementary directed mapping.}
\label{fig:track-to-world-transformation-support}
\end{figure}

This figure provides that forward-emission interpretation and closes the
operator pair with \cref{fig:world-to-track-transformation}. The
engineering consequence is that both mappings must be treated as coupled
runtime services with distinct numerical behaviour, not as a single
coordinate utility.

% ------------------------------------------------------------

\subsection{Planar Tangent and Normal Directions}

In the planar case:
\[
\mathbf t(s)
=
(\cos\theta(s),\sin\theta(s)),
\]
and:
\[
\mathbf n(s)
=
(-\sin\theta(s),\cos\theta(s)).
\]

% ------------------------------------------------------------

\subsection{Spatial Runtime Evaluation}

In spatial runtime evaluation, the mapping depends additionally on
profile, cant, frame construction, metric interpretation, and runtime
realization context. Consequently, the world embedding
\(x(s,d,h)\) is generally generated dynamically from runtime pose3
evaluation.

% ------------------------------------------------------------

\subsection{Reference-Line Interpretation}

The lateral coordinate \(d\) depends on the chosen reference
interpretation. Possible reference structures include the alignment
centerline, track center, rail axis, gauge reference, or operational
offset definitions. A more formal treatment of reference-line conventions
for multi-track systems, cant-dependent offsets, and vehicle-space
interpretation remains open.

% ============================================================
\section{World-to-Track Transformation}
% ============================================================

\begin{definition}[World-to-track projection]
\label{def:state-world-to-track-runtime}
\index{Projection!world-to-track}
The inverse transformation is:
\begin{equation}
(s^\ast,d^\ast)
=
\arg\min_{s,d}
\|
x-(\gamma(s)+d\,\mathbf n(s))
\|.
\label{eq:state-world-to-track-runtime}
\end{equation}
\end{definition}

This relation specializes the canonical world-to-track operator
\(\mathcal P_{\mathrm{wt}}\) introduced in
\cref{def:world-to-track-operator}.

The term projection is used operationally and does not necessarily imply
a unique orthogonal projection in the classical geometric sense.

Within AIM, world-to-track projection is interpreted as an inverse
runtime evaluation problem.

% ============================================================
\section{Coordinate Transformation versus Projection}

\index{Projection!distinction from coordinate transformation}
% ============================================================

Coordinate transformation and world-to-track projection are distinct
operations within AIM. Coordinate transformations map between world-space
representations, \(\mathcal C:\Omega_{\mathrm{world}}\to\Omega_{\mathrm{world}}\),
as introduced in \cref{def:coordinate-transform-operator}. World-to-track
projection maps between \(\Omega_{\mathrm{world}}\) and
\(\Omega_{\mathrm{track}}\).

\begin{principle}[Projection distinction principle]
\label{princ:projection-distinction}
\index{Projection distinction principle}
Coordinate transformation must not be confused with intrinsic
world-to-track projection.
\end{principle}

CRS transformations operate entirely within external coordinate
representations, whereas projection accesses intrinsic railway geometry.

% ============================================================
\section{Numerical Evaluation}
% ============================================================

The inverse transformation is generally nonlinear. Typical numerical
approaches include nearest-point search, local Newton iteration,
segment-wise projection, topology-aware lookup, and hybrid runtime
strategies. Projection quality depends strongly on curvature behaviour,
topology, initialization, runtime context, and metric interpretation.

% ============================================================
\section{Ambiguity and Context Dependence}

\index{Projection!ambiguity}
% ============================================================

World-to-track mapping is generally not globally unique. Ambiguities may
occur in loops, switches and crossings, parallel tracks, closely spaced
alignments, station areas, and topology-rich railway networks. Multiple
intrinsic states may correspond to nearly identical world-space
locations. Consequently, projection is fundamentally context dependent.
Runtime projection may require track identity, route identity, topology
context, expected station range, operational direction, runtime
interaction history, vehicle context, or network-state constraints.

\begin{principle}[Projection ambiguity principle]
\label{princ:projection-ambiguity}
\index{Projection ambiguity principle}
World-to-track projection cannot generally be interpreted as a purely
local geometric operation.
\end{principle}

% ============================================================
\section{Topology and Operational References}
% ============================================================

Intrinsic geometry alone is insufficient for operational railway
referencing. Operational interpretation additionally depends on
topology, route membership, kilometre references, operational
direction, and network semantics. A complete projection context may
therefore require:
\[
(
x,
\mathcal T,
\mathcal R,
\mathcal O
),
\]
where \(x\) denotes world-space input, \(\mathcal T\) topology context,
\(\mathcal R\) route/reference context, and \(\mathcal O\)
operational context.

% ============================================================
\section{Relation to Curvature}
% ============================================================

Projection stability depends directly on curvature behaviour. High
curvature increases projection sensitivity, whereas low curvature
typically yields stable projection behaviour. Errors in intrinsic
coordinates propagate nonlinearly into world space, and behaviour near
transition regions may depend strongly on higher-order curvature.

% ============================================================
\section{Runtime Dependency}
% ============================================================

Within AIM, world--track transformation depends on runtime evaluation of
the underlying alignment representation. In particular, cGeom defines
intrinsic geometry, pose2 defines tangent evolution, pose3 defines
runtime spatial interpretation, metric services define realized
embedding, CRS services define external representation, and runtime
operators evaluate resulting states. Projection therefore depends on the
currently active runtime evaluation pipeline. Different runtime
configurations may yield different projection results, for example
projected versus ellipsoidal evaluation, target versus realized
geometry, low-resolution versus high-resolution runtime, or simplified
versus vehicle-aware interpretation.

\begin{principle}[Runtime projection principle]
\label{princ:runtime-projection}
World--track transformation is a runtime evaluation problem operating on
alignment states rather than a static coordinate utility.
\end{principle}

% ============================================================
\section{LOD and Hybrid Evaluation}
% ============================================================

Exact world-to-track projection is computationally expensive. Large-scale
systems therefore require LOD-dependent evaluation, approximate
projection, topology-aware filtering, and hybrid runtime architectures.

\begin{proposition}[Selective projection principle]
\label{prop:selective-projection}
Exact projection should only be evaluated where high precision is
required.
\end{proposition}

A hybrid strategy combines precomputed samples for coarse lookup,
topology-aware candidate filtering, cached runtime states, and local
parametric refinement for precision. Such architectures are
particularly important for large railway networks, interactive
visualization, realtime vehicle-space evaluation, and multi-resolution
runtime systems.

% ============================================================
\section{Connection to CRS and Realization}
% ============================================================

The complete external transformation chain is:
\[
\text{CRS}
\rightarrow
\Omega_{\mathrm{world}}
\rightarrow
\Omega_{\mathrm{track}}.
\]
For high-precision applications, the alignment itself may be defined on
a reference ellipsoid rather than in a projected plane. Projection may
also depend on realized geometry:
\[
\gamma_{\mathrm{real}}(s)
\neq
\gamma_{\mathrm{target}}(s).
\]
Physical measurements should therefore reference realized rather than
purely analytical geometry. The distinction between
\[
\text{intrinsic geometry}
\quad\text{and}\quad
\text{external embedding}
\]
is fundamental within AIM.

% ============================================================
\section{Key Insight}
% ============================================================

\begin{proposition}
World--track transformation is an inverse runtime service rather than a
static coordinate conversion.
\end{proposition}

\begin{proposition}
Within AIM, intrinsic alignment space is primary, whereas world
coordinates are interpreted as external embeddings of alignment states.
\end{proposition}

\begin{proposition}
Projection is generally:
\begin{itemize}
\item nonlinear,
\item ambiguous,
\item topology-dependent,
\item runtime-dependent,
\item and context-sensitive.
\end{itemize}
\end{proposition}

% ============================================================
\section{Connection to AIM}
% ============================================================

\begin{summary}
World--track transformation connects intrinsic railway geometry with
external coordinate systems, topology, runtime evaluation, and measured
reality.

Its nonlinear, ambiguous, topology-dependent, and scale-dependent nature
makes it a central runtime component of the AIM framework.

Within AIM, projection is therefore interpreted not as static coordinate
conversion, but as contextual runtime reconstruction of intrinsic
railway states from external observations.

Efficient handling consequently requires:
\begin{itemize}
\item hybrid runtime architectures,
\item topology-aware projection strategies,
\item LOD-dependent evaluation,
\item sparse-state reconstruction,
\item and metric-aware runtime services.
\end{itemize}
\end{summary}
